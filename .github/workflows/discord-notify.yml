on:
  push:
    paths:
      - 'fr/**'

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Webhook
        run: |
          curl -H "Content-Type: application/json" \
          -d '{"content": "🚀 Mise à jour dans le dossier fr !"}' \
          ${{ secrets.DISCORD_WEBHOOK }}